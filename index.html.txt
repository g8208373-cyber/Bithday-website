<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday!</title>
    <style>
        /* Modern CSS Reset & Fonts */
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-color: #05050a;
            --text-main: #ffffff;
            --accent-glow: #8a2be2;
            --accent-secondary: #ff007f;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        /* Particle Background */
        #particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -1;
            pointer-events: none;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle infinite alternate;
        }

        @keyframes twinkle {
            0% { opacity: 0.2; transform: scale(0.8); }
            100% { opacity: 1; transform: scale(1.2); box-shadow: 0 0 10px #fff; }
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
        }

        .hero h1 {
            font-size: 4rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--accent-glow), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: glow 3s ease-in-out infinite alternate;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 1.2rem;
            font-weight: 300;
            letter-spacing: 2px;
            opacity: 0.8;
        }

        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            font-size: 0.9rem;
            opacity: 0.6;
            animation: bounce 2s infinite;
        }

        /* Journey/Memory Section */
        .journey {
            padding: 100px 20px;
            display: flex;
            flex-direction: column;
            gap: 60px;
            align-items: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 20px;
            width: 100%;
            text-align: center;
            transform: translateY(50px);
            opacity: 0;
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .card.visible {
            transform: translateY(0);
            opacity: 1;
            box-shadow: 0 10px 30px rgba(138, 43, 226, 0.1);
        }

        .card h2 {
            font-size: 2rem;
            margin-bottom: 15px;
            color: var(--accent-secondary);
        }

        /* Interactive Surprise Section */
        .surprise-section {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .btn {
            background: linear-gradient(45deg, var(--accent-glow), var(--accent-secondary));
            border: none;
            padding: 15px 40px;
            border-radius: 30px;
            color: white;
            font-family: inherit;
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 0 20px rgba(138, 43, 226, 0.4);
        }

        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 40px rgba(255, 0, 127, 0.6);
        }

        .hidden-message {
            margin-top: 40px;
            max-height: 0;
            overflow: hidden;
            opacity: 0;
            transition: all 1s ease;
        }

        .hidden-message.revealed {
            max-height: 500px;
            opacity: 1;
        }

        .heart {
            font-size: 5rem;
            margin: 20px 0;
            animation: heartbeat 1.5s infinite;
        }

        /* Animations */
        @keyframes glow {
            from { filter: drop-shadow(0 0 10px rgba(138,43,226,0.5)); }
            to { filter: drop-shadow(0 0 20px rgba(255,0,127,0.8)); }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        @keyframes heartbeat {
            0% { transform: scale(1); }
            14% { transform: scale(1.3); }
            28% { transform: scale(1); }
            42% { transform: scale(1.3); }
            70% { transform: scale(1); }
        }

        /* Responsive */
        @media (max-width: 600px) {
            .hero h1 { font-size: 2.5rem; }
            .card { padding: 30px 15px; }
        }
    </style>
</head>
<body>

    <!-- Background Stars -->
    <div id="particles"></div>

    <!-- Intro Section -->
    <section class="hero">
        <!-- EDIT NAME HERE -->
        <h1>Happy Birthday, [Name]!</h1>
        <p>Welcome to your special day...</p>
        <div class="scroll-indicator">Scroll Down ↓</div>
    </section>

    <!-- Scrollable Messages -->
    <section class="journey">
        <!-- EDIT MESSAGES HERE -->
        <div class="card">
            <h2>Another Trip Around the Sun</h2>
            <p>I just wanted to take a moment to celebrate you. You bring so much light and energy into the lives of everyone around you.</p>
        </div>
        
        <div class="card">
            <h2>You're Incredible</h2>
            <p>Whether it's your smile, your laugh, or just the way you see the world, there's nobody quite like you. I feel so lucky to know you.</p>
        </div>

        <div class="card">
            <h2>To the Future</h2>
            <p>I hope this year brings you everything you're dreaming of, and a whole lot of unexpected adventures too. You deserve the best.</p>
        </div>
    </section>

    <!-- Final Interactive Section -->
    <section class="surprise-section">
        <button class="btn" id="surpriseBtn">Tap for a Surprise</button>
        
        <div class="hidden-message" id="secretMessage">
            <div class="heart">💖</div>
            <h2>I hope you have the most amazing day.</h2>
            <p style="opacity: 0.8; margin-top: 10px;">(Now go eat some cake!)</p>
        </div>
    </section>

    <script>
        // 1. Generate Starry Background
        const particlesContainer = document.getElementById('particles');
        const numStars = 100;

        for (let i = 0; i < numStars; i++) {
            let star = document.createElement('div');
            star.classList.add('star');
            
            // Randomize position, size, and animation duration
            let size = Math.random() * 3 + 1; // 1px to 4px
            star.style.width = `${size}px`;
            star.style.height = `${size}px`;
            star.style.left = `${Math.random() * 100}vw`;
            star.style.top = `${Math.random() * 100}vh`;
            star.style.animationDuration = `${Math.random() * 3 + 2}s`;
            star.style.animationDelay = `${Math.random() * 2}s`;
            
            particlesContainer.appendChild(star);
        }

        // 2. Scroll Animation (Intersection Observer)
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.3 });

        document.querySelectorAll('.card').forEach(card => {
            observer.observe(card);
        });

        // 3. Interactive Surprise Button
        const btn = document.getElementById('surpriseBtn');
        const message = document.getElementById('secretMessage');

        btn.addEventListener('click', () => {
            message.classList.add('revealed');
            btn.style.display = 'none'; // Hides button after clicking
        });
    </script>
</body>
</html>